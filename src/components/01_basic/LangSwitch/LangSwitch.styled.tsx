import { styled } from "@mui/material";

export const ImgWrapperStyled = styled("div")({
    display: "flex",
    width: 20,
    height: 20,
    lineHeight: 0,

    img: {
        width: "100%",
    },
});
